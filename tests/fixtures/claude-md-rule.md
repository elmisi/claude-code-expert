# Project Rules

## Testing Requirements

Before committing any changes, you MUST:
1. Run the test suite: `npm test`
2. Ensure all tests pass
3. Check for any new warnings

If tests fail, fix them before committing. Do not use `--no-verify` to skip pre-commit hooks.
